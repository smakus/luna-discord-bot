services:
  kokoro-tts:
    build: .
    ports:
      - "8880:8880"
    restart: unless-stopped
    environment:
      - HF_HOME=/app/.cache/huggingface  # keep model cache inside container
    volumes:
      - hf-cache:/app/.cache/huggingface  # persist models across rebuilds

volumes:
  hf-cache: